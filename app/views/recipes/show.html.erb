<h1><%= @recipe.name %></h1>
<p><%= @recipe.description %></p>

Category: <%= @recipe.category.name %><br><br>

Ingredients:
<table>
  <tr>
    <th>Name</th>
    <th>Quantity</th>
  </tr>
<% @ingredients.each do |ingredient| %>
  <tr>
    <td><%= ingredient.name %></td>
    <td><% recipe_ingredient = RecipeIngredient.where(recipe_id: @recipe.id, ingredient_id: ingredient.id) %><%= recipe_ingredient.first.quantity %></td>
  </tr>
<% end %>
</table>

<%= link_to "Edit", edit_recipe_path(@recipe) %>
<%= link_to "Delete", recipe_path(@recipe), method: :delete %>
